<!doctype html>
<html>
  <head>
    <meta charset='utf-8' />
    <%= wicked_pdf_stylesheet_link_tag "pdf"%>
    <%= csrf_meta_tags %>
</head>
        <table>
        <tbody>
        <tr>
        <td style="text-align: left;"><%= image_tag Logo.last.image.url, size: "150x90" %></td>
        <td>
        <h3 style="text-align: left;"> <%= @formP.formulario.nome %> </h3>
        </td>
        <td style="text-align: right;">
        <h3><%= @formP.id %> </h3>
        </td>
        </tr>
        <tr>
        <td style="text-align: left;" colspan="2"><small>Identificador: <%= @formP.id %> Emitido por: <%= @user.nome %> filial: <%= @filial.nome %> em: <%= @answers.last.created_at.strftime("%d/%m/%y %H:%M:%S") %></small></td>
        <td style="text-align: right;">
        </td>
        </tr>
        </tbody>
        </table>
        <hr />
